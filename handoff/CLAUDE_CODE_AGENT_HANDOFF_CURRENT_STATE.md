# Claude Code Agent — Current State Handoff

Generated by Windsurf: 2026-04-26.
Audience: Claude Code Opus 4.7 Max + 10 sub-agents (see `CLAUDE_CODE_AGENT_TASK_PACKET.json`).

This handoff is **evidence-driven**. Every claim below is backed by an artifact
file in `artifacts/repo-sync/` or `artifacts/handoff-to-claude/`. Do not start
work until you have read every linked artifact.

## 0. Truth map (binding)

| Role | Local Path | Online Repo |
|------|-----------|-------------|
| `contract-kit-v17` / WebUI / Hub | `G:\Github\contract-kit-v17` | `https://github.com/Ghenghis/contract-kit-v17` |
| `kilocode` | `G:\Github\kilocode-Azure2` | `https://github.com/AiDave71/kilocode` |
| `zeroclaw` | `C:\Users\Admin\Downloads\VPS\zeroclaw` | `https://github.com/Ghenghis/zeroclaw` |
| `hermes-agent` | `G:\Github\hermes-agent-2026.4.13\hermes-agent-2026.4.13` | `https://github.com/Ghenghis/hermes-agent` |
| `open-webui` | _(no local — clone fresh)_ | `https://github.com/Ghenghis/open-webui` |
| Secrets | `C:\Users\Admin\Downloads\api` | _NEVER COMMIT_ |

## 1. Cross-reference verdict per repo

| Role | Local HEAD | Remote HEAD | Δ | Verdict |
|------|-----------|-------------|---|---------|
| contract-kit-v17 | `ca5d8f0` | `ca5d8f0` | 0/0 | ✅ EXACT_MATCH (dirty tree only) |
| kilocode-Azure2 (vs `aidave71/feat/azure-voice-studio`) | `67bd133` | `574658` | 551/3 | ⚠ DIVERGED |
| zeroclaw fileset | _no .git_ | `ff254b4` | uninit | 🔧 NEEDS_INIT |
| hermes-agent fileset | _no .git_ | `5ef0fe1` | uninit | 🔧 NEEDS_INIT |
| open-webui | _none_ | `8dae237` | missing | ❗ CLONE_REQUIRED |

Source: `artifacts/repo-sync/REMOTE_CROSS_REFERENCE.md`.

## 2. Required artifacts produced (12)

```
artifacts/repo-sync/
  LOCAL_LOCATION_INVENTORY.csv       ← machine-readable inventory
  LOCAL_LOCATION_INVENTORY.md        ← human-readable inventory
  REPO_FRESHNESS_MATRIX.csv          ← ahead/behind for every git repo
  REPO_FRESHNESS_MATRIX.md           ← per-repo plan
  REMOTE_CROSS_REFERENCE.md          ← 5 GitHub repos verified by ls-remote
  CONFLICT_RISK_MATRIX.md            ← per-op risk + mitigation
  CUSTOM_DAVEAI_FILES.md             ← protected paths (do-not-overwrite)
  REPACK_MERGE_MANIFEST.csv          ← 536 file SHA256 dedupe rows
  REPACK_MERGE_VERDICT.md            ← merge verdict (NOT_REQUIRED)

artifacts/handoff-to-claude/
  DOCS_DIAGRAMS_AUDIT.md             ← doc + diagram audit (PASS)
  STALE_CLAIMS_REPORT.csv            ← machine-readable scan results

handoff/
  CLAUDE_CODE_AGENT_HANDOFF_CURRENT_STATE.md   ← this file
  CLAUDE_CODE_AGENT_TASK_PACKET.json           ← agent assignments + missions
```

## 3. Critical findings (read before starting)

### 3.1 kilocode-Azure2 — **DIVERGED 551/3 vs truth-map remote**

- Truth-map remote is `AiDave71/kilocode`, NOT `Ghenghis/kilocode`.
- Local has 3 remotes configured: `origin=Ghenghis`, `upstream=Kilo-Org`, `aidave71=AiDave71`.
- Local `feat/azure-voice-studio` HEAD = `67bd133`.
- AiDave71 `feat/azure-voice-studio` HEAD = `574658` → 551 ahead, 3 behind.
- Local has **1 stash** that has not been inspected.
- Local has uncommitted untracked artefacts.
- 81 audit gates V01–V81 + 21 agent .md files + 20 service subdirs are ALL DaveAI-custom (see `CUSTOM_DAVEAI_FILES.md`).
- 70 ahead / **385 behind** Kilo-Org upstream — defer that batch.

### 3.2 ZeroClaw — fileset only at `Downloads\VPS\zeroclaw`

- No `.git` of its own. Parent `Downloads\VPS\.git` is empty (uninit'd, no remotes).
- Full Rust source: `Cargo.toml`, `crates/`, `src/`, `app/`, `components/`, `fuzz/`, `firmware/`, `docs/`, `Dockerfile`, `flake.nix`, `AGENTS.md`, `CLAUDE.md`.
- Remote `Ghenghis/zeroclaw` main = `ff254b4`.
- Required: `git init` → add origin → fetch → 3-way diff → `review/zeroclaw-import` branch.

### 3.3 Hermes — three fileset copies, none with git history

- Active (canonical per truth map): `G:\Github\hermes-agent-2026.4.13\hermes-agent-2026.4.13` (5,257 files / 167.8 MB / 30 dirs).
- Outer wrapper: `G:\Github\hermes-agent-2026.4.13` (6,945 files / 206.7 MB).
- Stale partial: `G:\Github\hermes-agent` (1,663 files / 30.1 MB).
- Remote `Ghenghis/hermes-agent` main = `5ef0fe1`.
- Required: clone fresh to `G:\Github\hermes-agent-fresh`, mirror active fileset, commit on `review/active-fileset-import`.

### 3.4 Open-WebUI — pure missing clone

- No local at all. Clone `Ghenghis/open-webui` (main = `8dae237`) to:
  - `G:\Github\open-webui`
  - `G:\Github\open-webui-current`

### 3.5 Secrets — quarantine

- `C:\Users\Admin\Downloads\api` contains 21 plaintext files (`.env*`, `*.txt` keys, `API_KEYS_VERIFICATION.md`, nested `env/`).
- HARD RULE: never commit. Run V42_SECRET_SCAN before every push.
- Import flow: vscode.SecretStorage (KiloCode) · Hub canonical settings (server-side `:8082` only) · Hermes systemd env file (mode 0600).
- After import: encrypted-archive plaintext, then delete.

### 3.6 DaveAI repacks

- v1 (`G:\Github\DaveAI_Ecosystem_Truth_Skills_Repack`) and v2 (`G:\Github\contract-kit-v17\DaveAI_Ecosystem_Truth_Skills_Repack-v2`) are **byte-identical** for all 535 shared files; only diff is a self-zip in v1.
- Verdict: `MERGE_NOT_REQUIRED_REPACK_REDUNDANT`. Archive v1.

### 3.7 VPS deployment workspace

- `C:\Users\Admin\Downloads\VPS` is a multi-project deployment workspace, not a single repo.
- `.git` is initialized but has no commits and no remotes.
- Top dirs include `zeroclaw/`, `zeroclaw-config/`, `hermes_folder/`, `daveai/`, `daveai-api/`, `daveai-v7/`, `agentic-ui/`, `agent-brain/`, `code/`, `nginx/`, `configs/`, `plans/`.
- Action: audit each subfolder; do not git-track the parent.

## 4. Hard rules (binding for every agent)

1. **No force push** to any remote.
2. **No destructive `git reset --hard`** without backup branch first.
3. **No commit of any file under `C:\Users\Admin\Downloads\api`**.
4. **No overwrite of any path** listed in `CUSTOM_DAVEAI_FILES.md` without explicit user approval.
5. All sync work happens on **review branches** (e.g. `chore/docs-v2.1.0`, `sync/aidave71-2026-04-26`, `review/zeroclaw-import`, `review/active-fileset-import`).
6. **Never** rebase `kilocode-Azure2` against `origin/main` (Ghenghis); only against `aidave71/feat/azure-voice-studio`.
7. **Never** auto-pull from `upstream/main` (Kilo-Org); cherry-pick only with the protected-file allowlist.
8. **Tests required** after every batch (gates listed per repo in `REPO_FRESHNESS_MATRIX.md`).
9. **Final verdict only from evidence files** in `artifacts/`.

## 5. Modes available

| Mode | When to use |
|------|-------------|
| `SAFE_MODE` | fetch-only + report; default for all agents |
| `REVIEW_MODE` | branch + sync attempt + tests + require user approval |
| `IDLE_MODE` | prepare plans while user away; produce evidence files only |
| `SLEEP_MODE` | overnight: SAFE_MODE on every repo, write status reports |
| `AGENT_CONTROLLED_MODE` | Claude/KiloCode agents may resolve conflicts under guardrails |
| `YOLO_MODE` | per-action approval only; **never** bypasses truth gates |

## 6. Final state Windsurf is delivering to Claude Code

| Item | Status |
|------|--------|
| Location truth map verified on disk | ✅ |
| 5 GitHub remotes verified via `ls-remote` | ✅ |
| Per-repo ahead/behind measured | ✅ (where git history exists) |
| Stale-claim scan complete | ✅ (0 strong hits) |
| Diagram cross-reference complete | ✅ (10/10 referenced, 0 orphans) |
| Repack dedupe complete | ✅ (verdict: not required) |
| Protected file list verified | ✅ (1 path was incorrect, fixed) |
| 12 required artifacts produced | ✅ |
| Audit gates V79+V80+V81 currently passing on `contract-kit-v17` | ✅ |

Windsurf verdict: `READY_FOR_CLAUDE_AGENT_SYNC`

Claude Code may begin the multi-agent sync work as defined in
`CLAUDE_CODE_AGENT_TASK_PACKET.json`.

## 7. Cross-reference back

- `artifacts/repo-sync/LOCAL_LOCATION_INVENTORY.md`
- `artifacts/repo-sync/REPO_FRESHNESS_MATRIX.md`
- `artifacts/repo-sync/REMOTE_CROSS_REFERENCE.md`
- `artifacts/repo-sync/CONFLICT_RISK_MATRIX.md`
- `artifacts/repo-sync/CUSTOM_DAVEAI_FILES.md`
- `artifacts/repo-sync/REPACK_MERGE_MANIFEST.csv`
- `artifacts/repo-sync/REPACK_MERGE_VERDICT.md`
- `artifacts/handoff-to-claude/DOCS_DIAGRAMS_AUDIT.md`
- `artifacts/handoff-to-claude/STALE_CLAIMS_REPORT.csv`
