/**
 * ProviderAdapter — Vercel AI SDK adapter over RoutingService.
 * TODO(Sprint 1.5): wire one provider live (Claude 4.5) + capabilities-aware routing.
 */

export class ProviderAdapter {
  // TODO(Sprint 1.5 / 2)
}
