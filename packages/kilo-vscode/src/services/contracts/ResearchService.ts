/**
 * ResearchService — Tavily / Semantic Scholar / GitHub Code Search via Hub proxy.
 * TODO(Sprint 3): privacy-preserving query rewrite, refs sidecar emit.
 */

export class ResearchService {
  // TODO(Sprint 3)
}
